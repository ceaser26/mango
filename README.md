# mango

Auto-deployed website workspace.

## Structure
- `sites/DD.MM.YY-project-name/` → one site per request
- `scripts/rebuild-index.sh` → regenerates root `index.html` with links

## Deploy
Push to `main` triggers GitHub Pages deploy.
